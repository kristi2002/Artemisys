# Script di servizio / debug — NON devono finire in produzione
db.php
debug.php
tmp_read_odt.php
rimuovi_barbara.php
prove_orari.php
testo.txt

# File di sviluppo
.git
.gitignore
.claude
*.md
.env
.env.*
!.env.example

# File di build/deploy: non devono stare nella webroot dell'app.
# (NB: i Dockerfile restano leggibili dal builder; qui li escludiamo
#  solo dal "COPY ." dell'immagine app.)
Dockerfile
db
docker-compose.yaml
docker-compose.yml
.dockerignore

# NB: sql/ NON è escluso: serve al build dell'immagine db (db/Dockerfile).
# Nell'immagine app i .sql restano comunque bloccati da .htaccess (403).
